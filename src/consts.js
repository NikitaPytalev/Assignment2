import path from 'path';

export const ROOT_DIR = path.parse(process.cwd()).root;
export const BROTLI_EXT = '.br';
export const INVALID_INPUT = 'Invalid input';
export const OPERATION_FAILED = 'Operation failed';